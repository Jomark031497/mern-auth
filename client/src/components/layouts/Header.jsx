import { AppBar, Toolbar, Typography } from "@material-ui/core";
const Header = () => {
  return (
    <AppBar>
      <Toolbar>
        <Typography variant="h5">MERN-TEMPLATE</Typography>
      </Toolbar>
    </AppBar>
  );
};

export default Header;
